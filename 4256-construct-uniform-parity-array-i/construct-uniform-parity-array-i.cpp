class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int odd = 0, even = 0;
        // int n = nums1.size();
        
        // for(auto &num : nums){
        //     if(num % 2) odd++;
        //     else even++;
        // }
        // if(odd == n || even == n) return true;
        // return (odd % 2 == 0 ? false)
        return true;
    }
};